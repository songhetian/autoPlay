# 打包说明

客服电脑没有 Python 环境时，建议直接交付 `dist\invoice_uploader` 目录。

## 本地打包

在项目根目录运行：

```bat
build_exe.bat
```

脚本会自动完成：

1. 安装 `PyInstaller` 和项目依赖
2. 安装 Playwright 的 Chromium 运行时
3. 按 `invoice_uploader.spec` 打包
4. 把 `ms-playwright` 浏览器运行时复制到成品目录

## 交付给客服

把整个目录发给客服：

```text
dist\invoice_uploader
```

不要只发其中的 `invoice_uploader.exe`，因为还依赖：

- `data`
- `ms-playwright`
- `PySide6` 相关动态库

## 首次验证

在一台没有 Python 的 Windows 机器上至少验证这几项：

1. 程序能正常启动
2. 能打开“智能获取元素”浏览器
3. 能保存元素到元素库
4. 能执行一次“仅解析并导出 Excel”

## 说明

程序运行时会优先查找随包携带的 `ms-playwright` 目录，不依赖客服电脑全局安装浏览器运行时。
