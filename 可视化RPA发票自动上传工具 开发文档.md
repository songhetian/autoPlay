# 可视化RPA发票自动上传工具 开发文档

**版本**：V1.0

**日期**：2026-03-26

**适用场景**：批量PDF发票自动化上传，适配多平台、多方案管理，支持可视化流程编排，无需专业技术基础即可操作

**核心定位**：小型可视化RPA工具，专门用于PDF发票批量上传，内置元素拾取、截图识图、搭积木式流程编辑功能，实现全流程自动化，降低人工操作成本，提升上传效率与稳定性

# 一、文档说明

## 1.1 文档用途

本文档为可视化RPA发票自动上传工具的完整开发指导文档，用于规范工具的开发、测试、部署与维护流程，明确工具的技术选型、功能设计、架构分层、界面规范及核心业务逻辑。文档不含代码实现，仅提供标准化开发依据，确保开发过程统一、高效，最终交付物符合需求预期。

## 1.2 核心功能概述

本工具以“可视化、无代码、高稳定”为核心，整合文件解析、网页自动化、图像识别、流程编排等能力，具体核心功能如下：

- 文件解析：读取指定文件夹内PDF发票，从固定命名规则中提取订单号，生成「订单号-文件绝对路径」字典；

- 可视化流程编排：采用搭积木式操作，拖拽添加流程步骤，无需编写代码即可完成上传流程配置；

- 元素管理：内置网页元素拾取器，自动获取网页元素定位信息，保存至元素库，支持随时调用与管理；

- 图像管理：支持截图、找图、等待图片等功能，截图自动保存至图片库，用于网页元素定位兜底，提升工具稳定性；

- 多方案管理：支持创建、保存、切换、删除多个上传方案，每个方案独立配置参数，适配不同业务场景；

- 跨平台支持：一次开发，适配Windows、Mac、Linux三大操作系统；

- 自动清理：发票上传成功后，自动删除对应源文件；上传失败则保留文件并记录日志；

- 实时监控：提供实时日志输出、进度统计、失败提醒，便于用户查看操作状态。

## 1.3 适用人群

开发人员（工具开发、维护）、测试人员（功能验证）、最终用户（发票上传操作人员，无需技术基础）

# 二、功能详细需求

## 2.1 发票文件解析模块

### 2.1.1 文件规则

- 文件格式：仅处理.pdf格式电子发票，自动过滤非PDF文件；

- 命名格式：固定为“dzfp_订单号_姓名_时间戳.pdf”；

- 示例文件名：dzfp_26312000001748879251_薛蓉蓉_20260323110527.pdf；

- 订单号提取：按下划线“_”分割文件名，提取第二段字符串作为订单号。

### 2.1.2 解析逻辑

1. 接收用户传入的本地文件夹路径（支持通过GUI界面选择文件夹）；

2. 遍历该文件夹内所有文件，过滤掉非.pdf格式的文件；

3. 对每个PDF文件，按下划线分割文件名，提取第二段作为订单号；

4. 生成键值对字典，格式为{订单号: 文件完整绝对路径}，用于后续上传流程调用；

5. 解析完成后，展示解析结果（订单号数量、文件路径列表），供用户确认；

6. 自动生成Excel文件，Excel中包含订单号、文件路径、处理状态三列，初始状态统一为“未处理”，Excel文件与发票文件夹同路径，命名格式为“发票上传处理记录表_时间戳.xlsx”。

## 2.2 可视化流程编排模块（核心RPA功能）

### 2.2.1 流程编辑方式

采用搭积木式可视化编辑，支持拖拽添加步骤、拖拽调整步骤顺序、删除步骤、编辑步骤参数，所有操作均在GUI界面完成，无需编写任何代码。

### 2.2.2 核心流程步骤（可自由组合）

- 基础步骤：打开浏览器、关闭浏览器、等待（固定时间等待、等待元素出现、等待元素消失、等待图片出现、等待图片消失）；

- 网页操作步骤：输入文本（关联元素库，选择输入框）、点击操作（关联元素库/图片库，选择按钮或图片位置）、网页刷新；

- 文件操作步骤：上传文件（自动填入文件路径）、删除文件（上传成功后执行）；

- 辅助步骤：截图（区域截图，保存至图片库）、日志输出、失败重试。

### 2.2.3 流程测试与运行

- 支持单步测试：选中单个流程步骤，点击“测试”按钮，验证步骤执行效果；

- 支持全流程运行：配置完成后，点击“开始执行”，自动遍历订单号字典，执行完整上传流程；

- 支持暂停/停止：运行过程中可随时暂停，暂停后可继续执行；也可直接停止流程，停止后记录当前执行进度。

## 2.3 元素库管理模块

### 2.3.1 元素拾取功能

- 内置网页元素拾取器，启动浏览器后，点击“拾取元素”按钮，鼠标移动至网页目标元素（输入框、查询按钮、上传按钮等），点击即可自动识别元素定位信息（无需手动编写XPATH/Selector）；

- 拾取后自动预览元素位置，确认无误后，可输入元素名称，保存至元素库；

- 支持多类型元素拾取：输入框、按钮、下拉框等常见网页元素均支持拾取。

### 2.3.2 元素库管理

- 元素库存储：所有拾取的元素信息以JSON格式本地存储，包含元素名称、定位方式、定位参数等；

- 元素操作：支持元素查询、编辑、删除、重命名，可按业务场景分类管理元素；

- 元素调用：流程编辑时，可直接从元素库中选择对应元素，无需重复拾取，提升流程配置效率。

## 2.4 图片库管理模块

### 2.4.1 截图功能

- 支持区域截图：点击“截图”按钮，拖动鼠标选择截图区域，完成截图后自动保存至图片库；

- 截图命名：可自定义截图名称，便于后续识别与调用；

- 截图预览：图片库中可预览所有截图，支持放大查看细节。

### 2.4.2 图片识别与调用

- 找图功能：流程中可添加“找图”步骤，从图片库中选择目标图片，设置相似度（0.8~1.0可调），工具自动在屏幕中查找匹配图片；

- 等待图片：支持“等待图片出现”“等待图片消失”步骤，用于处理网页弹窗、加载动画等场景，提升流程稳定性；

- 图片定位兜底：当网页元素定位失效时，可切换为图片定位，确保流程正常执行。

### 2.4.3 图片库管理

- 图片存储：截图文件以本地文件夹形式存储，图片库同步展示所有图片及对应信息（名称、存储路径、相似度默认值）；

- 图片操作：支持图片查询、删除、重命名，可按业务场景分类管理图片。

## 2.5 多方案管理模块

### 2.5.1 方案配置内容

每个方案独立保存以下配置信息，适配不同业务场景（如不同网页、不同发票文件夹、不同操作流程）：

- 基础配置：方案名称、发票文件夹路径、浏览器选择（Chrome/Edge）；

- 流程配置：已编排好的完整上传流程步骤；

- 元素与图片配置：关联的元素库、图片库（可指定专属元素/图片，也可共用公共库）；

- 运行配置：重试次数、等待时间（固定等待时间、元素/图片等待超时时间）；

- 清理配置：上传成功后是否删除源文件（开关控制）。

### 2.5.2 方案操作

- 新建方案：点击“新建方案”，配置相关参数，保存后生成新方案；

- 保存方案：编辑完成后，点击“保存方案”，更新方案配置信息；

- 切换方案：通过下拉框选择已保存的方案，加载对应配置（流程、元素、参数等）；

- 删除方案：选择不需要的方案，点击“删除方案”，彻底删除方案配置（元素库、图片库文件不删除）。

## 2.6 自动化上传模块

### 2.6.1 单张发票上传流程（默认基础流程）

1. 打开指定浏览器，进入发票上传网页；

2. 从元素库选择订单号输入框，输入当前订单号；

3. 从元素库选择查询按钮，点击查询，等待订单信息加载完成；

4. 从元素库选择上传发票按钮，点击后等待上传弹窗出现（可配置“等待图片出现”步骤，兜底弹窗定位）；

5. 在弹窗内，从元素库/图片库选择上传发票按钮，点击后触发Windows/Mac/Linux系统文件选择框；

6. 自动填入当前发票文件的绝对路径，按下回车确认文件选择；

7. 从元素库/图片库选择弹窗确认按钮，点击确认；

8. 等待上传成功（可配置“等待图片出现”步骤，识别上传成功图标）；

9. 若配置“上传成功删除文件”，则自动删除当前PDF源文件；若上传失败，保留文件并记录失败原因；

10. 同步更新Excel文件中对应订单行的处理状态：上传成功则标记为“成功”，上传失败（仅限订单号不存在场景）则标记为“失败”，失败后自动跳过当前订单，继续执行下一个订单的上传循环，不中断整体流程。

### 2.6.2 批量上传逻辑

遍历“订单号-文件路径”字典，对每个订单号重复执行单张发票上传流程，直至所有订单处理完成。运行过程中实时统计成功数量、失败数量，输出详细日志，同步更新Excel文件中对应订单的处理状态，失败场景（订单号不存在）自动跳过并标记状态，不中断循环。

## 2.7 日志与异常处理模块

### 2.7.1 日志输出

- 实时日志：GUI界面设置日志输出区，实时显示每一步操作结果（如“正在上传订单号：xxx”“文件上传成功”“已删除文件：xxx”）；

- 日志保存：所有日志自动保存至本地文件，支持后续查看、导出；

- 日志分类：按操作类型分类（解析日志、流程日志、错误日志），错误日志标红显示，便于快速定位问题；同步记录Excel文件生成及状态更新日志，明确每笔订单的状态变更原因。

### 2.7.2 异常处理



- 文件异常：文件不存在、路径错误、非PDF文件，自动跳过，记录日志并标记失败；

- 网页异常：网页加载超时、元素定位失败、图片查找失败，按配置的重试次数自动重试，重试失败则记录日志并标记失败；

- 订单异常：订单号查询无结果，记录日志并标记失败，保留对应文件，同步更新Excel中该订单状态为“失败”，自动跳过当前订单，继续下一次循环；

- 系统异常：系统文件选择框调用失败，提示用户手动操作，同时记录日志。

## 2.8 跨平台与文件清理模块

### 2.8.1 跨平台支持

工具支持Windows 10/11、Mac OS、Linux三大操作系统，核心功能（文件解析、流程编排、元素拾取、图片识别、上传操作）在各平台均能正常运行，仅系统文件选择框的调用逻辑根据操作系统做适配调整。

### 2.8.2 文件清理

- 清理触发：仅当发票上传成功后，自动触发文件删除操作；

- 清理逻辑：根据字典中的文件路径，删除对应PDF源文件，删除后在日志中记录“已删除：订单号xxx，文件路径xxx”；

- 异常保留：上传失败、流程中断的发票文件，均保留在原文件夹，便于用户后续重新处理。

# 三、技术选型

## 3.1 核心技术栈（最优匹配RPA需求，易开发、高稳定、跨平台）

|模块名称|技术选型|核心用途|
|---|---|---|
|图形界面（GUI）|Flet|搭建可视化界面，实现流程画布、元素库、图片库、日志区等组件，支持拖拽操作，跨平台兼容|
|网页自动化|Playwright|实现网页打开、元素定位、输入、点击等操作，自带元素拾取工具，无需手写定位代码|
|图像识别|opencv-python + pyautogui|实现截图、找图、等待图片、点击图片位置等功能，用于元素定位兜底，提升稳定性|
|流程编排与存储|JSON|存储流程步骤、方案配置、元素库信息，实现流程序列化与持久化|
|文件处理|Python os 内置库|遍历文件夹、解析文件名、生成文件路径、删除已上传文件|
|图片存储|本地文件夹|存储截图文件，与图片库联动，便于调用与管理|
|日志处理|Python logging 模块|实现日志实时输出、分类记录、本地保存|
## 3.2 技术选型说明

- Flet 优势：纯Python开发，无需编写HTML/CSS/JS，上手快，界面现代流畅，支持拖拽组件，打包体积小，跨平台兼容性好，完美适配RPA可视化流程编辑需求；

- Playwright 优势：相比Selenium，元素定位更稳定，自带元素拾取工具，支持多浏览器，跨平台能力强，无需额外配置驱动，开发效率更高；

- opencv-python + pyautogui 优势：图像识别精度高，支持相似度调节，可适配不同屏幕分辨率，能有效解决网页元素定位失效的问题，提升工具稳定性；

- JSON 优势：轻量、易解析、跨平台，适合存储流程配置、元素信息等结构化数据，无需依赖数据库，降低开发与部署成本。

## 3.3 依赖安装说明

核心依赖库安装命令（Python 3.8+ 版本）：

```bash
# 安装核心依赖
pip install flet playwright opencv-python pyautogui

# 安装Playwright浏览器驱动
playwright install
```

# 四、工具整体架构

## 4.1 架构分层（从上至下，职责清晰）

1. 界面层（GUI层）：基于Flet搭建，包含主界面、流程编辑画布、元素库面板、图片库面板、日志输出区、方案管理区等，负责用户交互与操作展示；

2. 流程调度层：负责流程步骤的解析、排序、执行，处理步骤间的依赖关系，实现单步测试、全流程运行、暂停/停止等功能，同步调用各底层模块；

3. 核心功能层：包含四大核心模块，分别是文件解析模块、元素管理模块、图片管理模块、自动化上传模块，负责实现工具的核心业务逻辑；

4. 基础支撑层：包含技术选型中的各类依赖库，提供网页自动化、图像识别、文件处理、日志记录等基础能力，为上层模块提供支撑；

5. 数据存储层：负责数据持久化，包括方案配置（JSON文件）、元素库（JSON文件）、图片库（本地文件夹）、日志文件（本地文本文件）。

## 4.2 数据流流程

本地文件夹 → 文件解析模块（生成订单号-路径字典）→ 流程调度层（加载方案、解析流程步骤）→ 核心功能层（调用元素库/图片库，执行网页操作、文件操作）→ 数据存储层（更新日志、保存配置）→ 输出结果（上传成功/失败、文件清理）

# 五、界面设计规范

## 5.1 界面整体布局

工具界面采用分栏布局，清晰划分功能区域，操作便捷，符合RPA工具使用习惯，整体布局如下：

- 顶部导航栏：包含工具名称、方案管理（新建/保存/删除/切换）、帮助（使用说明、版本信息）；

- 左侧面板：分为元素库、图片库两个标签页，可切换查看，支持元素/图片的查询、编辑、删除；

- 中间核心区：流程编辑画布，支持拖拽添加步骤、调整步骤顺序，显示当前流程的所有步骤及参数；

- 右侧面板：包含配置区（方案参数、运行参数、清理配置）、进度统计区（成功数、失败数、总数量）；

- 底部区域：日志输出区，实时显示操作日志，支持清空、导出日志。

## 5.2 核心界面详细设计

### 5.2.1 主界面

核心功能：方案切换、流程编辑、运行控制、结果查看，界面简洁明了，操作逻辑连贯，适合非技术人员使用。关键组件包括：

- 方案选择下拉框：显示所有已保存的方案，点击可切换；

- 方案操作按钮：新建方案、保存方案、删除方案；

- 流程控制按钮：开始执行、暂停执行、停止执行、单步测试；

- 进度条：显示当前批量上传的进度（百分比）；

- 统计面板：显示成功数量、失败数量、总订单数；

- 日志输出区：实时显示日志，错误日志标红，支持滚动查看。

### 5.2.2 元素拾取界面

弹出式界面，用于拾取网页元素，操作简单，无需技术基础：

- 浏览器选择：选择要启动的浏览器（Chrome/Edge）；

- 启动浏览器按钮：点击后启动指定浏览器，进入目标网页；

- 拾取元素按钮：点击后，鼠标变为拾取状态，移动至网页元素并点击，自动识别元素信息；

- 元素预览区：显示拾取到的元素信息（名称、定位方式、定位参数），可预览元素在网页中的位置；

- 保存按钮：确认元素信息后，输入元素名称，点击保存至元素库。

### 5.2.3 图片截取界面

弹出式界面，用于区域截图并保存至图片库：

- 截图按钮：点击后，屏幕变为截图状态，拖动鼠标选择截图区域；

- 截图预览区：显示截取的图片，支持放大查看；

- 图片名称输入框：输入截图名称，便于后续调用；

- 相似度设置：默认0.9，可调节（0.8~1.0），用于后续找图匹配；

- 保存按钮：点击后，将截图保存至图片库。

### 5.2.4 流程编辑界面（画布）

核心界面，用于搭积木式编辑上传流程：

- 步骤库：包含所有可添加的流程步骤（基础步骤、网页操作步骤、文件操作步骤、辅助步骤），支持拖拽添加至画布；

- 流程画布：显示当前编辑的流程步骤，每个步骤显示名称、参数，支持拖拽调整顺序、右键删除；

- 步骤参数编辑区：选中某个步骤后，显示该步骤的可配置参数（如元素选择、图片选择、等待时间、重试次数等），可直接编辑；

- 测试按钮：选中单个步骤，点击测试，验证步骤执行效果；

- 清空按钮：清空当前流程画布，重新编辑流程。

# 六、性能与稳定性要求

## 6.1 性能要求

- 批量处理能力：支持1~1000张PDF发票批量上传，单张发票上传耗时≤10秒（取决于网页加载速度）；

- 解析效率：文件夹内1000张PDF发票，解析时间≤30秒，订单号提取准确率100%（符合命名规则）；

- 界面响应：拖拽流程步骤、切换面板、操作按钮，响应时间≤1秒，无卡顿；

- 跨平台性能：在Windows、Mac、Linux系统中，运行流畅，无明显性能差异。

## 6.2 稳定性要求

- 元素定位稳定性：元素拾取准确率≥98%，定位失败后可通过图片定位兜底，流程中断率≤2%；

- 图片识别稳定性：图片匹配相似度≥0.8时，识别准确率≥95%，支持不同屏幕分辨率适配；

- 异常恢复能力：流程执行中出现异常（如网页崩溃、网络中断），支持断点续传，重新执行未完成的订单；

- 数据安全性：方案配置、元素库、图片库、日志文件均本地存储，不上传第三方服务器，确保数据安全；

- 长期运行稳定性：连续运行24小时，无内存泄漏、无崩溃现象，可正常处理批量发票上传。

# 七、安全与合规说明

- 数据安全：工具仅用于本地文件读取、网页自动化操作，不采集、不存储用户个人信息、发票敏感信息，所有配置文件、日志、图片均本地存储；

- 操作合规：完全模拟人工操作，不修改网页源代码、不绕过业务系统验证，符合企业业务系统使用规范；

- 文件安全：仅删除上传成功的PDF源文件，删除前无额外操作，失败文件保留，避免误删；

- 权限说明：工具运行仅需要本地文件读取/删除权限、浏览器调用权限，无需管理员权限（特殊系统除外）。

# 八、维护与扩展

## 8.1 可扩展点

- 流程步骤扩展：新增更多RPA常用步骤（如键盘操作、Excel导入/导出、邮件通知等）；

- 元素库扩展：支持自定义元素定位方式，支持批量导入/导出元素库；

- 图片库扩展：支持图片批量导入/导出，支持图片相似度批量调整；

- 方案扩展：支持方案批量导入/导出，支持方案分类管理；

- 报表扩展：新增上传结果报表生成功能，支持导出Excel报表；

- 浏览器扩展：新增对Firefox等其他浏览器的支持。

## 8.2 维护要求

- 元素维护：当网页元素变更时，重新拾取对应元素，更新元素库信息，无需修改流程步骤；

- 驱动维护：浏览器版本更新时，升级对应Playwright驱动，确保网页自动化功能正常；

- 依赖维护：定期更新核心依赖库，修复已知bug，提升工具稳定性；

- 日志维护：定期清理日志文件，避免占用过多磁盘空间，保留重要日志记录；

- 兼容性维护：适配新发布的操作系统版本，确保跨平台功能正常。

# 九、总结

本工具采用“可视化RPA”设计理念，以Flet为GUI框架，Playwright为网页自动化核心，opencv-python+pyautogui为图像识别支撑，实现了发票批量上传的全流程自动化。工具具备元素拾取、图片识别、搭积木式流程编辑、多方案管理、跨平台支持、自动文件清理等核心功能，无需专业技术基础即可操作，解决了传统自动化工具“需写代码、稳定性差、适配性弱”的痛点。

工具整体架构清晰、职责分明，开发难度适中，运行稳定可靠，可大幅提升发票上传效率，降低人工操作成本，适配多场景、多平台使用需求；新增Excel自动生成及状态标记功能，可清晰追溯每笔订单处理情况，失败场景（订单号不存在）自动跳过不中断流程，后续可通过扩展功能进一步提升工具的实用性与易用性。
> （注：文档部分内容可能由 AI 生成）